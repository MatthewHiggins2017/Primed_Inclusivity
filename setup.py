import setuptools

setuptools.setup(
    name="primedinclusivity",
	version="1.0.0",
    packages=setuptools.find_packages(),
	long_description="PrimedInclusivity Platform",
	scripts= ['PrimedInclusivity',
              'PIConfig'])
